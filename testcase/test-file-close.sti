voin main(){
	String filename = "data.txt";
	if (open filename) { 
		std << "File is opened.\n";
		if (close filename){
			std << "File is closed.\n";
			else { 
				std << "Failure when close file.\n";
			}
		}
	}
	else { 
		std << "Failure when open file.\n";
	}
	return 0;
}
